[package]
name = "amqp0-primalgen"
authors = ["Alexander Reece <github@reece45.com>"]
version = "0.0.2"

[features]
default = []

[dependencies]
inflections = "1.0.0"
amqp0-specs = { version = "0.0.4", path = "../specs" }

[dependencies.clippy]
version = "*"
optional = true
